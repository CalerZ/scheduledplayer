<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 存储权限 (Android 13 以下) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    
    <!-- 音频文件访问权限 (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <!-- 精确闹钟权限 (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- 开机自启权限 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 振动权限 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 保持唤醒 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 通知权限 (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 忽略电池优化 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 蓝牙权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <application
        android:name=".NoiseRetaliationApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoiseRetaliation"
        tools:targetApi="34">

        <!-- 主界面 -->
        <activity
            android:name=".presentation.ui.main.MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 任务创建/编辑界面 -->
        <activity
            android:name=".presentation.ui.task.TaskEditActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.main.MainActivity" />

        <!-- 华为权限引导界面 -->
        <activity
            android:name=".presentation.ui.permission.HuaweiPermissionGuideActivity"
            android:exported="false"
            android:theme="@style/Theme.NoiseRetaliation" />

        <!-- 任务执行日志界面 -->
        <activity
            android:name=".presentation.ui.log.TaskLogActivity"
            android:exported="false"
            android:parentActivityName=".presentation.ui.task.TaskEditActivity" />

        <!-- 音频播放前台服务 -->
        <service
            android:name=".service.player.AudioPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- 开机启动广播接收器 -->
        <receiver
            android:name=".service.scheduler.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- 华为快速启动 -->
                <action android:name="com.huawei.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- 闹钟广播接收器 -->
        <receiver
            android:name=".service.scheduler.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- WorkManager 初始化 -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
